# Gradle 빌드 성능 설정 (로컬 개발·Docker 빌드 공통 적용).

# 빌드 캐시 — 입력이 같은 태스크의 결과를 재사용한다.
# 캐시는 Gradle 홈에 쌓이므로, Docker 빌드에서는 Dockerfile의 캐시 마운트와 함께 동작한다.
org.gradle.caching=true

# 병렬 실행 — 의존관계가 없는 태스크를 동시에 돌린다.
org.gradle.parallel=true
